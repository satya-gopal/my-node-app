version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: "my-node-app-task"
        LoadBalancerInfo:
          ContainerName: "my-node-app"
          ContainerPort: 3000
        PlatformVersion: "LATEST"
